# --- core ---
numpy>=1.26
pandas>=2.2
pyarrow>=15.0          # parquet 저장
scipy>=1.12
pyyaml>=6.0
python-dotenv>=1.0

# --- http ---
requests>=2.32
tenacity>=8.2          # 재시도 백오프

# --- deep learning ---
torch>=2.2             # macOS: MPS 백엔드 사용

# --- eval / viz ---
matplotlib>=3.8
scikit-learn>=1.4

# --- dev ---
pytest>=8.0
ruff>=0.4
tqdm>=4.66

# Phase 2 (Mamba) 진입 시에만 설치:
# mamba-ssm  (CUDA 전용 — Mac에서는 순수 PyTorch 대체 구현 필요)
