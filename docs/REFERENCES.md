# 레퍼런스 — 무엇을 가져왔고, 무엇을 우리가 정했나

이 프로젝트의 모델은 **한 논문의 변형이 아니다.** 서로 다른 계보 셋에서 부품을
떼어 조립했고, 조립 이후의 설계 판단은 논문이 아니라 **이 저장소의 실측**으로 내렸다.

이 문서는 그 둘을 분리해서 적는다.

- **1~3절** — 외부에서 가져온 것과 검토한 대안 (출처가 있다)
- **4~5절** — 우리가 정한 것 (출처가 없고, 대신 측정치와 커밋이 있다)

> ⚠️ **서지정보 미검증.** 1절 표의 저자·학회·연도는 원문 대조를 아직 못 했다
> (2026-09-10 확인 시도, 검색 불가). **발표·제출 전에 원문으로 확인할 것.**
> 저장소 안에서 검증된 사실은 "우리가 쓴 기법이 무엇인지"까지이고,
> "그 논문의 정확한 서지정보"는 아직 아니다.

---

## 1. 아키텍처 계보 — 부품별 출처

`src/models/phase1.py` 의 조립 순서대로.

| 부품 | 파일 | 출처 (미검증) | 연도 |
|---|---|---|---|
| RevIN 인스턴스 정규화 | `revin.py` | Kim et al., *Reversible Instance Normalization for Accurate Time-Series Forecasting against Distribution Shift*, ICLR | 2022 |
| Patch 토큰화 · 채널 독립 | `patch_embed.py` | Nie et al., *A Time Series is Worth 64 Words* (**PatchTST**), ICLR | 2023 |
| Self-Attention 인코더 | `encoder.py` | Vaswani et al., *Attention Is All You Need*, NeurIPS | 2017 |
| 글로벌-로컬 크로스어텐션 | `cross_attention.py` | **없음 — 우리 설계** (4절) | — |
| 변수 선택망 (VSN) · GRN | `vsn.py` | Lim et al., *Temporal Fusion Transformers* (**TFT**), Int. J. Forecasting | 2021 |
| 분위 헤드 · Pinball Loss | `quantile_head.py`, `losses.py` | Koenker & Bassett, *Regression Quantiles*, Econometrica (1978) / TFT 의 분위 헤드 | 1978 |

**요약: PatchTST 백본 + TFT 해석 부품.** 흔히 "TFT 변형"이라 부르지만 정확하지 않다.
TFT 에서 가져온 것은 VSN·GRN·분위 헤드뿐이고, 시퀀스를 다루는 등뼈는 PatchTST 계열이다.

## 2. 각 부품을 왜 골랐나 — 요구사항에서 역산했다

루트 [`CLAUDE.md`](../CLAUDE.md) 가 선언한 차별점 두 개가 아키텍처를 강제했다.
**"최신이라서" 고른 부품은 하나도 없다.**

| 요구사항 | 강제되는 것 | 결과 |
|---|---|---|
| **기권 로직** — "지금 판단해도 되는가" | 점추정이 아니라 **분포**가 필요하다. q90−q10 폭이 있어야 기권이 정의된다 | 분위 헤드 + Pinball |
| **해석가능성** — 리포트 근거 | post-hoc(SHAP) 가 아니라 **학습된** 피처 중요도 | TFT VSN |
| 종목마다·시기마다 스케일이 다름 | 비정상 시계열 정규화 | RevIN |
| lookback 120 × 146종목 학습 비용 | 시퀀스 길이 축소 (120 → 24토큰) | Patch 토큰화 |

TFT 는 2021년 논문이라 최신이 아니다. 그래도 남긴 이유는 **"변수 중요도를 게이트로
학습한다"는 아이디어의 대체재가 아직 없기 때문**이다. 최신성이 아니라 기능으로 골랐다.

### 두 요구사항이 충돌한 지점 — 채널 독립을 유지한 이유

PatchTST 의 channel-independence 를 그대로 쓴 게 아니라 **VSN 을 살리려고 선택했다.**
`patch_embed.py:8` 에 그대로 적혀 있다 — *"채널을 섞으면 TFT 변수선택망이 가중할
대상이 사라져 해석가능성 근거가 없어진다."* 두 계보를 잇는 실제 이식 지점이 여기다.

## 3. 검토만 하고 재지 않은 대안

| 대안 | 연도 | 왜 안 썼나 |
|---|---|---|
| DLinear / TSMixer | 2022~23 | 분위 3개를 내게 고칠 수는 있으나 **해석가능성 근거가 없다.** 베이스라인으로만 유효 |
| 시계열 파운데이션 모델 (Chronos, TimesFM, Moirai 등) | 2024~25 | 실질 독립 사건이 **거래일 ~1,900일**뿐이라 파인튜닝 규모가 안 된다. 0.33M 도 과적합한 데이터다(5절). 또한 "AI 원클릭"을 피한다는 프로젝트 최우선 목표와 정면 충돌 |
| Mamba / SSM 병렬 expert | 2024 | **폐기 아님 — Phase 2 스트레치 목표**. `src/models/CLAUDE.md` 참조. `mamba-ssm` 이 CUDA 전용이라 Mac 재구현이 필요하다 |

## 4. 출처가 없는 부분 — 우리가 설계한 것

**`cross_attention.py` 는 논문에서 가져오지 않았다.** 국내 종목 시퀀스를 Query,
매크로/글로벌 지표 시퀀스를 Key/Value 로 두는 구조를 직접 정했다. 방향이 설계의
핵심이다 — 매크로가 Query 가 되면 출력이 매크로 시퀀스 모양이 되어 종목별 예측이
안 나온다(`cross_attention.py:6`).

정직하게 적으면: 외생변수를 어텐션으로 주입한다는 발상 자체는 새롭지 않고,
**TimeXer(2024)가 같은 문제를 논문으로 다뤘다.** 우리는 그걸 참고해서 만든 게 아니라
독립적으로 도달했다. 발표에서 "우리 고유 기여"라고 주장하려면 이 사실을 함께 밝혀야 한다.

## 5. 설계 판단의 실측 근거 — 논문이 아니라 커밋이다

아키텍처를 고른 뒤의 모든 변경은 이 저장소에서 측정해서 결정했다.
각 행의 커밋을 `git show <해시>` 로 열면 근거가 나온다.

| 판단 | 근거 | 측정치 | 커밋 |
|---|---|---|---|
| **VSN 을 인코더 앞에 둔다** | 인코더가 채널 수(17)만큼 반복 실행됨 | forward **259ms → 52ms** (5배) | `057485c` |
| **헤드를 무조건부 분위수로 초기화** | 기본 초기화는 출력이 타깃보다 9배 큰 상태에서 시작 | val pinball 0.013993 → 기준선 0.013285 **대비 -5.3%** 였다 | `35d7bca` |
| **모델 크기 minimal(0.33M) 확정** | 1.88M 은 2 epoch 째부터 기준선보다 나빠짐 (즉시 과적합) | 기준선 대비 **+3.35%**, 블록 부트스트랩 **2.8σ** | `02a140f`, `4c7f468` |
| **매크로는 채널을 먼저 합쳐 한 번만 인코딩** | 채널별 인코딩 시 비용 13배, VSN 이 없어 채널 보존 이유 없음 | — | `057485c` |
| **분위 단조성을 손실이 아니라 구조로 보장** | 교차가 나면 `trading/signal.py` 가 예외를 던져 백테스트가 죽는다 | 누적 softplus 방식 | `0a2ea6d` |
| **절대 임계값 → 횡단면 순위** | train 이 2022 하락장으로 끝나 절대 임계값이 무너짐 | — | `d40d53e` |
| **트랜스포머 vs GBDT** | 베이스라인 대조 + 순열검정 | 손실은 이겼고 **백테스트는 못 이겼다.** 신호 기여 Sharpe +0.12, **p=0.25** | `8cdf853` |
| **목표를 방향에서 불확실성으로** | 방향 IC vs 폭 IC | **0.024 (t=3.9)** vs **0.294 (t=43.8)** — 폭이 12배 | `547a6fb` |

**모든 실험은 무조건부 분위수 기준선과 비교한다.** `train.py` 가 매번 같이 재서
리포트에 남긴다. 이 기준선이 이 프로젝트의 실질적 레퍼런스다 —
못 이기면 하이퍼파라미터가 아니라 피처/타깃 설계 문제로 본다
([`src/training/CLAUDE.md`](../src/training/CLAUDE.md)).

## 6. 모델 밖의 인용

| 어디 | 무엇 | 쓰임 |
|---|---|---|
| `src/trading/record.py:108` | Boyd et al., *Markowitz Portfolio Construction at Seventy*, 2024, §2.1 | 총자산 대비 비중 정의 — 제약 `1ᵀw + c = 1`. 현금을 잔차로 잡는 근거 |
| [`docs/LIMITATIONS.md`](LIMITATIONS.md) | 자본시장연구원(KCMI), 「AI와 자산운용: 패러다임의 전환 가능성 고찰」 | 4대 모델리스크(설명가능성·안전환상·행태수렴·데이터편향)를 한계 문서의 뼈대로 사용 |

이 둘은 **저장소에서 실제로 인용 형태로 존재하는 유일한 출처**다.
1절의 모델 논문들은 코드 docstring 에 이름만 있고 서지정보가 없다 — 그래서 이 문서를 만들었다.

---

## 7. 그리드 플로우 트랙 — 원문 대조 완료 (2026-09-21)

> ✅ **1절과 달리 이 절의 서지정보는 원문 PDF 를 직접 열어 확인했다.**
> 저자·소속·연도·데이터·수수료·결론을 본문에서 옮겼다. 발표에 그대로 인용해도 된다.

### 7.1 그리드 트레이딩의 계보

**학술 논문에서 나온 기법이 아니다.** FX 실무에서 쓰이던 방식이 코인 거래소 봇으로
대중화된 것이고, 프리퀀트(frequant.kr/grid)도 그 구현체 중 하나다. **발표에서 이걸
먼저 인정하고 들어간다** — 숨기면 "이미 있는 거 아니냐"에서 무너진다.

다만 학술적 뿌리는 분명하다.

| 출처 | 그리드와의 관계 |
|---|---|
| Avellaneda & Stoikov, *High-Frequency Trading in a Limit Order Book*, **Quantitative Finance 8(3), 217–224, 2008** | 그리드 = **재고(inventory)를 무시한 순진한 대칭 마켓메이킹**. A-S 는 재고와 변동성에 따라 호가를 기울이는 최적해를 닫힌 형태로 유도한다. 우리 그리드에는 그 재고 항이 없다 — 한계로 명시할 것 |

### 7.2 Chen, Chen & Jang (2025) — 기대값 0 증명 ★ 발표의 중심축

> Kai-Yuan Chen, Kai-Hsin Chen, Jyh-Shing Roger Jang (국립대만대 NTU),
> *Dynamic Grid Trading Strategy: From Zero Expectation to Market Outperformance*,
> **arXiv:2506.11921v1 [q-fin.TR], 2025-06-13**

- **데이터**: Binance 현물 1분봉, **2021-01 ~ 2024-07**, BTC·ETH. 수수료 **0.0008(8bp)**
  (OKX Level 1 maker) 반영. 분당 최대 1체결 가정
- **그리드 형태**: 등비(geometric) 그리드
- **핵심 증명 — 가격이 직선 이동하면 기대값이 음수다**

  $$E(G)=\tfrac12 (P_u - L_l) = -\tfrac{M}{n}\Big(\tfrac{n^2}{8}-\tfrac{n}{4}\Big)$$

  상승 시 이익 $P_u$ 보다 하락 시 손실 $L_l$ 이 크다. 이유는 **그리드가 상한에 닿기
  전에 이익을 실현해버리고, 하락할 때는 더 많이 사기 때문**이다. 그 음수를 왕복
  차익이 메워서 **전통 그리드의 총 기대값 = 0**
- **DGT 개선안**: 범위를 벗어나도 **중단하지 않고 그리드를 재설정**한다.
  상단 돌파 시 원금 회수 후 재투자, 하단 이탈 시 코인을 들고 차익 수익을 새 원금으로
- **결과**: IRR 60~70%. BTC 는 매수후보유 대비 IRR 높고 MDD 크게 낮음.
  ETH 는 IRR 은 비슷하나 **MDD 를 80% → 50% 로** 억제
- **인용할 한 문장**:
  > "a small grid size results in lower profits due to the high proportion of
  > transaction fees within the arbitrage profit"

**우리 실측이 이 논문을 한국 주식에서 독립적으로 재현했다** — 이게 발표의 강한 카드다.

| 논문 (코인, 왕복 8bp) | 우리 (코스피, 왕복 33.8bp) |
|---|---|
| 기대값 ≈ 0 | 노출통제군 대비 고유분 **−1.34%p** |
| 작은 간격은 수수료에 먹힘 | 0.5% 간격 = 통제군 대비 **−34.31%** |
| MDD 80% → 50% | 하이닉스 하락 구간 **−5.67% vs 보유 −14.83%** |
| 최적 간격은 균형점 | **간격 = 왕복비용 × 4.5** (프리퀀트 실제 설정 역산값과 일치) |

### 7.3 Yeh, Hsieh & Huang — 가변 그리드 + ANN ★ 우리 설계와 가장 가깝다

> Wei-Chang Yeh, Yu-Hsin Hsieh (국립칭화대 NTHU), Chia-Ling Huang (개남대),
> *Newly Developed Flexible Grid Trading Model Combined ANN and SSO algorithm*,
> **arXiv:2211.12839**, 32쪽

- **데이터**: S&P 500 · NASDAQ Composite · DJIA · Euro Stoxx 50 · 상해종합
  **5개 대형지수, 2011~2022 (10년)**
- **문제의식**: 기존 그리드는 **진입·청산이 너무 이르다**(premature entry and exit)
- **가변 그리드(flexible grid)의 정체 — 위아래를 따로 설정한다**
  1. 위 칸 수 $n_u$ 와 아래 칸 수 $n_l$ 을 **독립적으로** 지정 (비대칭 사다리)
  2. 위 간격비 $G_{su}$ 와 아래 간격비 $G_{sl}$ 을 **따로** 지정.
     $0 < G_{su} < 1$, $G_{sl} > 1$ 로 두어 **중심에서 멀어질수록 칸이 촘촘해진다**
     → 급등하면 매도가 더 자주, 급락하면 매수가 더 자주 일어난다
     (일반 등비 그리드는 멀어질수록 **넓어진다** — 정반대다)
- **파라미터 최적화**: SSO(Simplified Swarm Optimization, Yeh 2009)로 구간별 최적
  $(G_{ul}, G_{ll}, n_u, n_l, G_{su}, G_{sl})$ 을 찾고, **그 해를 라벨로** FNN·LSTM 을
  학습시켜 "최근 시장 정보 → 최적 그리드 파라미터"를 내놓게 한다
- **결과**: 같은 칸 수·같은 상하한에서 flexible > 등차 > 등비.
  S&P 500 ROI **31.69% vs 27.93% vs 22.23%** (상하한 ±30% 기준).
  FNN 쪽이 Sharpe 에서 특히 우수

**우리 계획과의 관계**: "모델이 그리드 파라미터를 정한다"는 구조가 이미 문헌에 있다.
다만 이 논문은 **SSO 로 찾은 최적해를 지도학습으로 모사**하고, 우리는 **모델의
`q90−q10` 을 변동성 예측으로 직접 쓴다** — 그 차이가 우리 기여다.
그리고 이 논문은 **지수**를, 우리는 **개별 종목**을 다룬다(종목 선별 문제가 추가된다).

### 7.4 그 밖에 확인한 것

| 출처 | 내용 | 대조 |
|---|---|---|
| *ML-Augmented High-Frequency Grid Trading: Strategy-Embedded Labeling, Soft Martingale Execution, and Drawdown Dichotomy Quantification*, **Algorithms (MDPI) 2025**, doi:10.3390/a19060442 | lot-scaling 으로 최대 자본 노출 63% 감축하면서 평균회귀 복원 메커니즘 유지 | 초록만 확인. **원문 미대조** |

### 7.5 이 절에서 우리가 정한 것 (출처 없음)

| 무엇 | 값 | 근거 |
|---|---|---|
| 간격 = 왕복비용 × 4.5 | 키움 기준 1.5% | 프리퀀트 설정 역산(0.363%÷0.08%)과 자체 격자 실측이 독립 수렴 |
| 로스컷 −15% | | 10% 이하는 사이클 수익 +1.31%→+0.08% 로 붕괴. 일봉 트랙의 손절 교훈과 같은 방향 |
| 종목 선별 = 변동성 상위 | | 고변동 상위 5 가 전종목 대비 고유분 +0.84%p→+1.20%p. **"횡보 종목" 기준은 오히려 나빴다** |
| 재배치 하루 1회 | | 10분·30분·1시간·하루 비교에서 방향성 없음(92~121%, 잡음) |

### 7.6 위아래 비대칭(`grid.skew`) — 재고 기각 (2026-09-22)

모델의 분위가 한쪽으로 치우쳤으면(`q90−q50` ≠ `q50−q10`) 사다리 배분도 그쪽으로
기울이는 게 맞지 않나 — 전체 범위는 그대로 두고 **배분만** 바꾼다.
`src.trading.grid.spans_from_quantiles(skew="quantile")` 로 구현하고
일봉 198종목(q50 상위 5) 에서 대칭과 **사이클마다 짝지어** 비교했다.

| 사이클 | 구간 | n | 고유분 차이(모델 − 대칭) | t | p | 이긴 사이클 |
|---|---|---|---|---|---|---|
| 5거래일 | val | 49 | +0.006%p | +0.54 | 0.59 | 59% |
| 5거래일 | **test** | 132 | +0.008%p | +0.73 | 0.47 | **48%** |
| 10거래일 | val | 25 | −0.003%p | −0.10 | 0.92 | 44% |
| 10거래일 | test | 65 | +0.056%p | +1.97 | 0.05 | 60% |

**효과가 없다.** 채택 사이클(5거래일)에서 test 는 절반도 못 이겼고 p 는 0.47 이다.
10거래일 test 만 단독으로 유의에 가깝지만 같은 설정의 val 이 음수라,
"val·test 양쪽에서 이겨야 한다"는 이 프로젝트의 기준을 못 넘는다.

원인은 **모델의 분위가 거의 대칭**이라는 것이다 — 평균 기울기가 +0.083 에 그쳐
사다리가 실질적으로 안 기운다. 신호 없이 그냥 기울인 대조군(s=±0.2)이 같은 수준을
내는 것도 같은 이야기다(5일 val 에서는 대조군이 더 낫다).

그래서 기본값은 `skew: none` 이다. 코드와 테스트는 남긴다 — 되살릴 때 다시 짜지
않기 위해서이고, `cvar_limit: null` 과 같은 처리다. 재시도한다면 기울기를 상수로
곱해 증폭하는 쪽이지, 분위 비율을 그대로 쓰는 지금 방식이 아니다.
측정 스크립트: 스크래치패드 `skew_daily.py`.

### 7.7 사이클 중간의 폭 재조정 — 채택 (2026-09-22)

폭을 사이클 시작에 한 번만 정하면 그 사이 시장이 바뀌어도 사다리가 안 따라간다.
모델은 매일 새 예측을 내므로 **매일 폭을 다시 계산**하고, 변화가 `relay_band` 를
넘을 때만 미체결 칸을 취소하고 다시 건다. **기준가는 안 옮긴다** — 옮기면 그리드가
아니라 매일 재시작이고, 사이클 안의 저점매수·고점매도가 성립하지 않는다.

예외 하나(사용자 결정 2026-09-22): 가격이 사다리 맨 위·맨 아래 칸 **바깥**이면
(`grid.outside_ladder`) 체결될 칸이 없으므로 현재가로 사이클을 새로 연다.
⚠️ **실측하지 않았다** — 아래 표는 이 예외가 없던 시점의 값이다.

예측 폭은 실제로 움직인다: 같은 종목의 `q90−q10` 이 5거래일 뒤 중앙 7.6%,
상위 10% 는 22.8% 달라진다. 재조정할 여지가 있다는 뜻이다.

사이클마다 짝지은 고유분 차이(일봉 198종목, q50 상위 5, ratio 0.6, α=0.20):

| 사이클 | 구간 | n | 모델 재조정 − 고정 | t | p | 이긴 사이클 |
|---|---|---|---|---|---|---|
| **5거래일** | **val** | 49 | **+0.049%p** | +4.47 | **0.000** | 59% |
| **5거래일** | **test** | 132 | **+0.037%p** | +3.35 | **0.001** | 58% |
| 10거래일 | val | 25 | +0.085%p | +2.21 | 0.037 | 72% |
| 10거래일 | test | 65 | +0.047%p | +0.91 | 0.368 | 71% |

**채택 사이클(5거래일)에서 val·test 양쪽이 유의하다.** 이 프로젝트에서 두 구간을
모두 통과한 몇 안 되는 변경이다.

밴드는 0.10 으로 잡았다. 밴드 0(매일 재배치)과 성과가 같은데 재배치 횟수는
사이클당 4.0 → 1.1 회로 준다 — 취소·재주문은 API 호출이고, 잡음만큼 흔드는 것은
비용만 남는다.

⚠️ **대조군은 이겼다고 말할 수 없다.** 같은 규칙을 직전 20일 실현변동성으로 돌린
대조군과는 차이가 없다(5일 val +0.007%p p=0.57 / test −0.003%p p=0.81).
**이득의 출처는 "매일 다시 잰다"는 행위 자체이고, 무엇으로 재는지는 가르지 못했다.**
모델을 쓰는 근거는 (a) 초기 폭에서는 모델이 고정 대비 확인된 우위가 있고(7.5),
(b) 같은 예측 한 번에서 나오므로 추가 비용이 0 이라는 것이다. 발표에서 이 표를 뺀
"모델이 시장에 맞춰 폭을 조정합니다"는 과장이다.

**같이 들어간 안전장치**: 폭이 좁아지면 짝 매도(한 칸 위)가 매입가 밑으로 내려올 수
있다. `paired_sell_price(..., fill_price, costs)` 가 **매입가 + 왕복비용** 을 바닥으로
건다 — 없으면 재조정할 때마다 확정 손해 자리가 생긴다.

측정 스크립트: 스크래치패드 `dyn_daily.py`.

### 7.8 네 조건 전부를 모델에 물린다 — 사용자 결정 (2026-09-22)

발표 자료의 "그리드 간격 변경을 유발하는 조건" 네 가지를 모델 출력에 그대로 맵핑했다.

| 조건 | 모델이 쓰는 값 | 설정 |
|---|---|---|
| 01 최근 가격 변동성 | `q90−q10` → 칸 간격, 매일 재계산 | `width_alpha` `relay_band` |
| 02 현재 주가 위치 | `q50` → 사다리 **중심**을 그 자리로 옮긴다 | `center_k` `center_cap` |
| 03 추세 지표 | `q90−q50` vs `q50−q10` → 위/아래 배분 | `skew: quantile` |
| 04 거래비용·리스크 | 왕복비용 하한·상한·로스컷 (제약이지 예측이 아니다) | `floor_mult` 등 |

**조건을 하나씩 켜면서 잰 값** (일봉 198종목, 고유분 = 노출통제군 대비):

| 조합 | 5일 val | 5일 test | 10일 val | 10일 test |
|---|---|---|---|---|
| ① 변동성만(사이클 고정) | +0.12%p | +0.12%p | +0.39%p | +0.20%p |
| **①+ 매일 재조정** | **+0.17%p** | **+0.16%p** | **+0.48%p** | +0.25%p |
| ①+② 중심=q50 | +0.17%p | +0.16%p | +0.48%p | **+0.30%p** |
| **①+②+③ 네 조건 전부(채택)** | +0.15%p | +0.13%p | +0.46%p | +0.26%p |

짝지은 차이 — **네 조건 전부 − 매일재조정만**:
5일 val **−0.026%p (p=0.01)** · test **−0.033%p (p=0.00)** / 10일 val −0.017%p(p=0.32) · test +0.009%p(p=0.75).

**대가는 5일 사이클 고유분 약 −0.03%p 다**(1억 기준 사이클당 3만원 수준, 유의하다).
원인은 조건 03(`skew`)이고, 조건 02 는 중립이다(총수익은 오히려 +0.26 → +0.27%).

⚠️ **이걸 켠 것은 성과 근거가 아니라 사용자 결정이다** — "네 조건 전부 모델이 판단하게
한다". 값어치가 숫자로 확인된 것은 조건 01(폭·재조정) 하나뿐이고, 위 표를 발표에서
빼지 않는다. 되돌리려면 `skew: none`, `center_k: 0` 두 줄이면 된다.

근거 수치의 출처는 계획서 `~/.claude/plans/virtual-crafting-heron.md` 와
스크래치패드 프로토타입(`grid_sim.py` 외)이다. G1~G2 에서 `src/` 로 옮기면
그때부터는 저장소 안의 테스트가 근거가 된다.

논문 : https://arxiv.org/pdf/2211.12839
