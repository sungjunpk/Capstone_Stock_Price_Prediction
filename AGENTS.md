# AGENTS.md — Codex 등 다른 에이전트용

이 저장소의 **정본은 따로 있다.** 여기에 규칙을 다시 적지 않는다 — 두 군데 적으면 반드시 어긋난다.

| 무엇 | 어디 |
|---|---|
| 절대 규칙 · 작업 원칙 | [`CLAUDE.md`](CLAUDE.md) ← **먼저 읽는다** |
| 진행 상황 · 다음 할 일 | [`README.md`](README.md) |
| 폴더별 상세 | `src/*/CLAUDE.md` (작업할 폴더 것만) |

이 문서는 **그 규칙들을 Claude Code 밖에서 지킬 때 달라지는 점**만 담는다.

---

## ⚠️ 여기서는 안전장치가 없다

`CLAUDE.md` 의 절대 규칙 중 넷은 글이 아니라 `.claude/hooks/` 가 **기계적으로** 막고 있다.
그 훅은 Claude Code 에서만 돈다. **Codex 에서는 아무것도 안 막는다** —
아래를 스스로 지켜야 한다.

| 훅이 막던 것 | Codex 에서 직접 해야 할 일 |
|---|---|
| `KIWOOM_ENV=live` 실행·대입을 **차단** (규칙 1) | 절대 쓰지 않는다. `mock` 뿐이다 |
| `.env` 편집을 **차단** (규칙 2) | 열지도 고치지도 않는다. 값이 대화·커밋에 남는다 |
| 저장소 밖 `git` 명령을 **차단** | 이 맥에는 원격이 다른 저장소가 여럿 있다. `git` 은 이 폴더에서만 |
| `paper_trade --execute` 에 **확인 요구** (규칙 9) | **사용자 승인 없이 실행하지 않는다.** 실제 주문이 나간다 |
| `git commit` 전 **pytest 자동 실행**, 깨지면 커밋 거부 | 커밋 전에 `.venv/bin/python -m pytest -q` 를 **직접** 돌린다 |
| 파이썬 편집 후 **ruff 자동 실행** | 편집한 파일에 `.venv/bin/ruff check <파일>` 을 직접 돌린다 |
| 편집 시 확인을 요구하던 파일들 | 아래 파일은 고치기 전에 사용자에게 묻는다 |

확인을 요구하던 파일과 그 이유:

- `src/utils/config.py` — `KIWOOM_ENV=live` 를 막는 방어선이다 (규칙 1). 이 방어를 푸는 변경인지 확인
- `src/trading/signal.py` · `src/trading/risk.py` · `src/models/inference.py` — 백테스트와
  모의투자가 **공유하는 단일 구현** (규칙 7). 여기를 바꾸면 진행 중인 실거래 기록(2026-08-26~)의
  체제가 바뀐다
- `configs/config.yaml` — 매매 임계값의 단일 소스. `trading`/`backtest` 섹션을 바꾸면
  진행 중인 실거래 기록이 두 체제로 쪼개진다

`.claude/settings.json` 의 실행 허용목록도 Codex 에는 적용되지 않는다.

---

## 세션 시작

```bash
cd ~/Desktop/Capstone_Stock_Price_Prediction
bash .claude/hooks/session_brief.sh | jq -r '.hookSpecificOutput.additionalContext'
git log --oneline -8
git status --short
```

첫 줄이 패널 최신성·실거래 현황·리밸런싱 주기·수집 정상 여부를 한 줄로 찍는다.
(훅으로도 쓰이지만 **독립 실행된다.** Claude Code 가 없어도 그대로 돈다.)

그다음 `README.md` 의 **현재 상태** 표와 **다음 할 일** 을 읽는다.
`.venv/bin/python -m pytest -q` 가 통과하면 코드는 건강한 상태다.

---

## 두 에이전트를 번갈아 쓸 때

### 동시에 돌리지 않는다

- **같은 파일을 동시에 고치면 서로의 변경을 덮어쓴다.** 한쪽이 끝나고 커밋한 뒤 넘긴다.
- **수집·모의투자는 특히 위험하다.** 키움은 appkey 당 토큰을 하나만 살려둔다 —
  두 프로세스가 동시에 API 를 쓰면 나중에 발급한 쪽이 앞의 토큰을 무효화한다.
  종목이 조용히 유실된 사고가 실제로 있었다 (`README.md` 의 자동화 함정 표).

### 넘길 때 (Claude → Codex, 또는 반대)

1. 하던 일을 **커밋한다.** 커밋 메시지가 곧 작업 이력이다 (`CLAUDE.md` 작업 원칙).
2. 커밋할 수 없는 중간 상태라면 `README.md` 의 **다음 할 일** 맨 위에 한 줄 남긴다 —
   무엇을 하다 멈췄고, 검증이 어디까지 됐는지.
3. 새 파일을 만들어 인계장을 쓰지 않는다. 진행 상황은 `README.md` 하나다.

### 받을 때

위 "세션 시작" 절차 그대로. `git log` 의 최근 커밋 메시지가 직전 작업의 요약이다.

---

## 이 프로젝트에서 자주 틀리는 자리

`README.md` 에 사고 기록이 표로 정리돼 있다. 그중 코드를 고칠 때 걸리기 쉬운 것:

- **look-ahead** (규칙 5) — 새 피처가 t 시점 이후 정보를 참조하지 않는지. 조용히 틀리는 실패라
  피처·타깃·분할·백테스트를 건드렸으면 반드시 확인한다
- **정규화 통계는 train 구간에서만** (규칙 6) — val/test 를 통계에 넣으면 미래를 훔쳐본다
- **수집은 증분(idempotent)** (규칙 4) — 같은 명령을 두 번 돌려 중복 행이 생기면 안 된다
- **실험 결과는 덮어쓰지 않는다** (규칙 8) — `outputs/reports/` 에 날짜+설정 해시로 남긴다
- **MPS 는 범위 밖 임베딩 인덱스에 예외를 안 낸다** — 0 벡터를 조용히 돌려준다.
  CPU 는 `IndexError` 를 낸다. 텐서 인덱스 문제를 MPS 에서 디버깅하지 말 것

---

## 개발 환경

파이썬 3.11, 가상환경 `.venv/`. 명령은 `.venv/bin/python` 으로 직접 부르는 편이 안전하다
(`source .venv/bin/activate` 가 세션 간에 유지되지 않는 도구가 있다).
설정은 `configs/config.yaml` 단일 소스 — 코드에 숫자를 하드코딩하지 않는다.
