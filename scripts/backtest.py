#!/usr/bin/env python
"""backtest 엔트리포인트 — 아직 미구현.

Phase 1 모델(src/models/) 구현 후 채운다.
CLAUDE.md 규칙: 모델 구조 작업은 Plan Mode 로 설계 승인 후 진행할 것.
"""

import sys

print(
    "미구현: src/models/ (RevIN → Patch → Encoder → CrossAttn → VSN → QuantileHead) "
    "가 완성된 뒤 작성한다."
)
sys.exit(1)
